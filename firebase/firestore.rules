rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: any authenticated user can read/write.
    // Collection path: /users/{userId}/serviceSheets/{sheetId}
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
